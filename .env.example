# Server-side only. Never expose this key through NEXT_PUBLIC_* variables.
OPENROUTER_API_KEY=

# Automatically selects an available free model. Pin a specific :free model if needed.
OPENROUTER_MODEL=openrouter/free
